{
  "name": "alarm-api",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --test"
  },
  "dependencies": {
    "@ivanphz/workdays-core": "1.0.0"
  }
}