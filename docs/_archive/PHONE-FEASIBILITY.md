# PHONE-FEASIBILITY.md — 执行器可行性验证（V12 前置门禁）

> 目的: 在写内核代码前，用一个 10 分钟的测试快捷指令验证手机端全部原语。
> 每条原语都有既定备选，**任何失败都不阻塞架构，只改变落点方案**。
> 请逐条执行并回填「实测记录」，尤其 P2 的原文文本——那是关键数据。

---

## P1 词典原语（契约13的地基）

**测试**: 新建快捷指令 →「词典」动作，键 `do_not_disturb`，值 `勿扰模式` →「获取词典值」key=do_not_disturb →「显示结果」。
**预期**: 显示"勿扔模式"。词典是快捷指令原生动作，理应必过。
**实测记录**: ______

## P2 守卫比对（唯一真正需要本地化名称的地方）

**测试**:「获取当前专注状态 (Get Current Focus)」→「文本」强转 →「显示结果」。
按下表矩阵逐格执行，**抄录原文**:

| 手机状态 \ 系统语言 | 中文 | 英文 |
|---|---|---|
| 勿扰开 | ______ | ______ |
| 睡眠开 | ______ | ______ |
| 全部专注关闭 | ______(空?) | ______ |

再串联: If「文本」==「获取词典值(do_not_disturb)」→ 显示"命中"。
**判定**: 中英文均能取到稳定文本 → **A案**（契约13照准: token+手机词典，换语言=改词典一处）。取不到或不稳 → **B案**（见文末）。
**实测记录**: ______

## P3 last_applied 持久化（契约4的地基）

**测试**: 两条路线任一通过即可:
- 路线1「存储到文件」: 写词典 `{"focus":"on"}` 到 Shortcuts 文件夹 → 读回 → 取值比较。
- 路线2 Data Jar: 同上。
**实测记录**: 路线__ 通过 / 失败原因 ______

## P4 token 分支设置（证明"设置"根本不需要名称）

**测试**: If 变量 mode == `do_not_disturb` → 分支内放「设定专注状态」动作，**在编辑器里用选择器绑定"勿扰"**，开。
**要点**: 快捷指令的 Set Focus 是编辑期绑定的选择器，不吃运行时字符串——所以 token 只负责选分支，设置动作永不需要本地化名。这条验证后，本地化问题就收窄到 P2 一处。
**实测记录**: ______

## P5 读媒体音量（加强守卫，可选）

**测试**:「获取设备详细信息」→ 当前音量 → 显示。
**判定**: 读到 → media_volume 可配 only_if_current；读不到 → 按契约5降级，默认防线不受影响。
**实测记录**: ______

## P6 通知通道预研（V13 notices，顺带）

**测试**: 自动化(时间触发、关闭"运行前询问") → 「显示通知」。锁屏状态下是否弹出。
**实测记录**: ______

## P7 换语言回归（一次性）

**测试**: 系统语言切换后重跑 P2。
**预期**: 内置专注名随语言变（词典改一处）; **自定义命名的专注模式名称不随语言变**（若某天想零维护，可自建一个自定义专注替代内置勿扰——记录在案，不强求）。
**实测记录**: ______

---

## 判定汇总

| 结果 | 落点 |
|---|---|
| P1+P3+P4 过 | 契约4/13 照准，执行器契约成立 |
| P2 过 | **A案**: token + 手机词典（默认方案） |
| P2 不稳 | **B案**: `?lang=` 时信封在 token 旁附加 `mode_label` 便利字段（token 仍是唯一权威值与 last_applied 比较基准; 翻译小表活在 edge/assemble，内核零感知） |
| P5 过/不过 | 加强守卫 开/降级 |
| P6 结果 | 决定 V13 notices 通道形态 |
